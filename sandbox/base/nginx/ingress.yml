apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-sample
spec:
  rules:
  - http:
      paths:
      - backend:
         service:
            name: ingress-sample-service
            port:
              number: 80
        path: /
        pathType: Prefix
