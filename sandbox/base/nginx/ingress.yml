apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sample
spec:
  rules:
  - http:
      paths:
      - backend:
         service:
            name: ingress-sample
            port:
              number: 80
        path: /
        pathType: Prefix
